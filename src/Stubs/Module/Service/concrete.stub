<?php

namespace {{ namespace }};

{{ uses }}

/**
 * @extends {{ base_class }}<{{ model }}>
 */
class {{ class }} extends {{ base_class }} implements {{ interface }}
{
    public function __construct({{ repository_type_hint }} $repository)
    {
        parent::__construct($repository);
    }
}
